# PyTorch CPU wheels live on this index; keep this line first so pip resolves `torch` correctly.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi
uvicorn[standard]
transformers
pydantic
torch==2.5.1+cpu
nltk
taaled
